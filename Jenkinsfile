pipeline {
    agent any

    stages {
        stage('Docker Login') {
            steps {
                echo "Docker Login"
            }
        }
        stage('Build Docker Images') {
            steps {
                echo "Build Docker Images"
            }
        }
        stage('Push Docker Images') {
            steps {
                echo "Push Docker Images"
            }
        }
        stage('Terraform') {
            steps {
                echo "Terraform"
            }
        }
    }
}
